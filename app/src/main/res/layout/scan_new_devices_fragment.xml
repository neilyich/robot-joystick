<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/list_title"
        android:padding="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/search_new_devices_title"
        android:textSize="16sp"
        android:layout_toStartOf="@id/btn_progress"
        android:layout_alignParentTop="true"
        android:layout_alignBottom="@id/btn_progress"
        android:layout_alignParentStart="true"
        android:gravity="center_vertical">
    </TextView>

    <pl.droidsonroids.gif.GifImageView
        android:id="@+id/btn_progress"
        android:layout_marginEnd="20dp"
        android:padding="6dp"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:backgroundTint="@color/primary"
        android:gravity="center"
        android:src="@drawable/dual_ring_small"
        android:contentDescription="refresh">
    </pl.droidsonroids.gif.GifImageView>

    <ListView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/list_title"
        android:layout_above="@id/btn_back">
    </ListView>

    <Button
        android:layout_margin="20dp"
        android:id="@+id/btn_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textAllCaps="false"
        android:text="@string/stop_search_and_back">
    </Button>



</RelativeLayout>